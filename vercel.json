{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/generate-ticket", "dest": "/app.js" }
  ]
}
